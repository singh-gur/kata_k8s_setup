# Kata Containers k3s Setup - Configuration
# Copy this file to .env and adjust values for your environment.
#
#   cp .env.example .env

# ---------------------------------------------------------------------------
# Kata version (docker image tag for kata-deploy)
# See: https://quay.io/repository/kata-containers/kata-deploy?tab=tags
# ---------------------------------------------------------------------------
KATA_VERSION=3.12.0

# ---------------------------------------------------------------------------
# SSH configuration (used by prereq-check and node debugging recipes)
# ---------------------------------------------------------------------------
SSH_USER=ubuntu
# SSH_KEY=/path/to/your/key    # Uncomment if not using ssh-agent
# SSH_OPTIONS="-o StrictHostKeyChecking=accept-new -o ConnectTimeout=10"

# ---------------------------------------------------------------------------
# Node configuration
# If left empty, worker nodes are auto-discovered from the cluster via kubectl.
# Set explicitly if kubectl node IPs don't match your SSH-reachable addresses.
# ---------------------------------------------------------------------------
# WORKER_NODES=10.0.1.10,10.0.1.11,10.0.1.12,10.0.1.13

# ---------------------------------------------------------------------------
# Kubernetes namespace for kata-deploy (default: kube-system)
# ---------------------------------------------------------------------------
# K8S_NAMESPACE=kube-system

# ---------------------------------------------------------------------------
# Dry-run mode - set to "true" to preview changes without applying
# ---------------------------------------------------------------------------
# DRY_RUN=false
