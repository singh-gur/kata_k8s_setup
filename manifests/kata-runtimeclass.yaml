---
# RuntimeClass for Kata Containers (uses QEMU hypervisor by default)
# The "kata" name provides a simple user-facing alias; the handler maps
# to the kata-qemu runtime registered by kata-deploy 3.26.0+.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: kata
handler: kata-qemu
